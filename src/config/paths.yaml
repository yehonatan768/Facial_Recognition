# Paths for data and outputs
paths:
  project_root: ""              # optional; resolved at runtime
  images_root: "images"
  pairs_train: "assets/pairsDevTrain.txt"
  pairs_val: "assets/pairsDevTest.txt"   # optional; if empty, split from train
  pairs_test: "assets/pairsDevTest.txt"
  outputs_dir: "outputs"
